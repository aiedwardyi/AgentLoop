# Calculator

Users report two bugs in `calculator.html`: arithmetic results are wrong, and the `%` button does nothing.

Work in small increments, one per cycle:

1. Fix the arithmetic.
2. Fix the `%` button.
